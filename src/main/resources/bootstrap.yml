server:
  port: 8090
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/David110690/BOOTCAMPNTTDATA.git
          searchPaths: config-data
          cloneOnStart: true

