server:
  port: 8090
spring:
  application:
    name: config-server
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/David110690/BOOTCAMPNTTDATA
          searchPaths: config-data
          cloneOnStart: true
          default-label: "master"
